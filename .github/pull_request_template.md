## What changed

<!-- One or two sentences. What does this do that the previous behaviour did not? -->

## Why

<!-- The problem being solved. Link an issue if there is one. -->

## Checklist

- [ ] `just check` passes locally (fmt, validate, lint, docs)
- [ ] READMEs regenerated with `just docs` if any variable or output changed
- [ ] The title of this pull request reads well as a release note
- [ ] Labelled `breaking` if it changes the `config` schema, and called out above
